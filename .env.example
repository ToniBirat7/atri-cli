# Root environment overrides for docker-compose.yml
# Copy to .env and replace dev defaults for production use.

LLAMA_API_KEY=__SET_ME__
ORCHESTRATOR_API_KEY=__SET_ME__
ORCHESTRATOR_JWT_SECRET=replace-with-a-long-random-secret
